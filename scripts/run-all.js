// each script just executes on its own, so requiring just runs everything
const split = require('./split-events');
const players = require('./players');
const teams = require('./teams');
const ball = require('./ball');